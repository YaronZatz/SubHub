# Public (client-safe) – required for Firebase client SDK
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=

# Server-only – never prefix with NEXT_PUBLIC_
# Firebase Admin: paste the full JSON from Firebase Console → Project Settings → Service accounts → Generate new private key
ADMIN_SDK_CONFIG=
GEMINI_API_KEY=
APIFY_API_TOKEN=
APIFY_WEBHOOK_URL=
NEXT_PUBLIC_APP_URL=http://localhost:3000
